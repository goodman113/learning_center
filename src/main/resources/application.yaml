spring:
  application:
    name: learningCenter
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    url: jdbc:postgresql://localhost:5432/learning_center
    username: postgres
    password: root123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
aws:
  s3:
    bucket: learning-center-uploads
    region: eu-north-1
    access-key: AKIA5SE3DSUARHE423RL
    secret-key: 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


springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
